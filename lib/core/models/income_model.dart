import 'package:flutter/material.dart';

class IncomeModel {
  final Color color;
  final String title, amount;

  const IncomeModel(
      {required this.color, required this.title, required this.amount});
}
